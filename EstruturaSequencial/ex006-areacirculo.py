# Nota: FORMULA CIRCULO: A = pi . r^2
PI = 3.14
raio = float(input('Digite o raio do círculo: '))
medida = input('Digite a unidade de medida do raio: ')
area = PI * raio ** 2
print('A área do círculo é: ', area, medida, 'aproximadamente')